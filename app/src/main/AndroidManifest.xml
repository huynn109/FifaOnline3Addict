<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.huynn109.fifaonline3addict">

  <uses-permission android:name="android.permission.INTERNET"/>

  <application
      android:name=".service.MyApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher_fo3_data"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme.Black">
    <activity
        android:name=".ui.activity.MainActivity"
        android:label="@string/app_name"
        android:windowSoftInputMode="stateHidden">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
    <activity android:name=".ui.activity.PlayerDetailActivity"/>
    <activity android:name=".ui.activity.FilterActivity"/>
    <activity android:name=".ui.activity.FilterSeasonActivity"/>
    <activity android:name=".ui.activity.FilterLeagueActivity"/>
  </application>

</manifest>